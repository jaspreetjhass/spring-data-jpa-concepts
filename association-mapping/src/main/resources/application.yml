spring:
  application:
    name: association-mapping
  datasource:    url: jdbc:mysql://localhost:3306/mydb
    username: root
    password: root
  jpa:
    hibernate:      ddl-auto: create    show-sql: true
    properties:      format-sql: true